# Xcode
# The .xcodeproj is generated by XcodeGen from project.yml (run `xcodegen generate`)
# and not checked in, since Xcode rewrites it locally (e.g. signing team) on every open.
MusicPractice.xcodeproj/
build/
DerivedData/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
*.xcuserstate
*.xcscmblueprint

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods (not used, but ignore if introduced)
Pods/

# macOS
.DS_Store
